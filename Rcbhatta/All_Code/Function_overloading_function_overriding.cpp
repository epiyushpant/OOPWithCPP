/*-------function overloading -------------------*/
        /*- in function overloading we decleare more then on
        function with same name and different types of parameter
        eg int sum();
        int sum(int x)
        - It can occuer without inheritance.
        - It function overloading must hace different signature of function
        - It is a concept of compile time polymorphsm
        - In overloading every function behaviour differently depending 
        upon parameter passed to them.
        */

/*----------------*Function overriding--------------------------*/
/*          - in function overriding we declear a function in base class
            and  derived class with same name and same parameters
            for ex 
            void sum();
            void sum();
            - It occurs when one class is inherited from another class (inheritance)
            - In function overriding must hace same signature of function
            - It is the concept of runtime polymorphism  
            - in function overriding every class function explain its own behaviour
            */